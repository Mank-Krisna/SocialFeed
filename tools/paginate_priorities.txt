Priority Analysis — Pagination Scan
====================================

P0 — Hardcoded paginate(N) not using config
--------------------------------------------
1. app/Livewire/AdminDashboard.php:65  ->paginate(10, pageName: 'usersPage')
2. app/Livewire/AdminDashboard.php:67  ->paginate(10, pageName: 'postsPage')
   → Replace with config('feed.per_page') for consistency.

P1 — Missing feed-sentinel element in Blade
--------------------------------------------
3. resources/views/livewire/feed.blade.php:60 only has HTML comment.
   feed-sentinel.js expects <div id="feed-sentinel"> — add it.

P1 — Livewire components missing queryString/resetPage
-------------------------------------------------------
4. app/Livewire/Feed.php — no $queryString for filter, no resetPage().
5. app/Livewire/GroupDetail.php — no $queryString, no resetPage().
   → Add protected $queryString = ['filter'] (Feed) / ['page'] if paginator used.
   → Call resetPage() in loadMore()/setFilter().

P2 — AdminDashboard incomplete queryString
-------------------------------------------
6. app/Livewire/AdminDashboard.php:23  $queryString = ['tab']
   → Missing 'page' for paginated users/posts lists.

Order of Fixes:
  P0 → P1 → P2
  1. AdminDashboard paginate(N) → config()
  2. Add #feed-sentinel element to feed.blade.php
  3. Feed/GroupDetail queryString + resetPage
  4. AdminDashboard queryString += 'page'
