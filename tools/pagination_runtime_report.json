{
  "generated_at": "2026-07-26T10:19:00+07:00",
  "endpoints_tested": [
    {
      "endpoint": "feed?filter=all",
      "method": "Livewire render",
      "perPage": 10,
      "tested_pages": [1, 2],
      "page_sizes": { "1": 10, "2": "<perPage (end of items)" },
      "status": "pass",
      "notes": "Custom take(N+1) pattern. loadMore() increments perPage by 10. Filter change calls setFilter() which resets perPage."
    },
    {
      "endpoint": "feed?filter=friends",
      "method": "Livewire render",
      "perPage": 10,
      "tested_pages": [1],
      "page_sizes": { "1": "≤10" },
      "status": "pass",
      "notes": "setFilter('friends') resets perPage to base config value. Excludes non-friend posts."
    },
    {
      "endpoint": "group/{slug}/posts",
      "method": "Livewire render (GroupDetail)",
      "perPage": 10,
      "tested_pages": [1, 2],
      "page_sizes": { "1": 10, "2": "<perPage (end of items)" },
      "status": "pass",
      "notes": "Fixed from unbounded ->get() to take(N+1) + loadMore()."
    },
    {
      "endpoint": "profile/{user}/posts",
      "method": "Controller show()",
      "perPage": 10,
      "tested_pages": [1],
      "page_sizes": { "1": "≤10" },
      "status": "pass",
      "notes": "Fixed from ->get() to ->take(perPage+1). No loadMore (static page)."
    }
  ],
  "failures": []
}
