<?php

use Illuminate\Support\Facades\Route;

// API routes placeholder — Sanctum auth will be added in V6
Route::middleware('auth:sanctum')->group(function () {
    // Future: POST /api/posts, GET /api/feed, etc.
});
